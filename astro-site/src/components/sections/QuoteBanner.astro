---
// Quote banner section - Premium animated version
---

<section class="section--primary quote-banner">
  <!-- Animated background elements -->
  <div class="quote-bg-elements" aria-hidden="true">
    <div class="quote-glow quote-glow-1"></div>
    <div class="quote-glow quote-glow-2"></div>
    <div class="quote-particles">
      <span></span>
      <span></span>
      <span></span>
      <span></span>
      <span></span>
    </div>
  </div>
  
  <div class="quote-content reveal">
    <h3 class="quote-title">
      <span class="quote-line">Entre la soledad del freelance</span>
      <span class="quote-line">y la complejidad de la agencia...</span>
    </h3>
    <p class="quote-highlight">¡Decidimos quedarnos justo en medio!</p>
  </div>
</section>

<style>
  .quote-banner {
    position: relative;
    min-height: 320px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 80px 24px;
    background: linear-gradient(135deg, #1a3865 0%, #234b7a 50%, #1a3865 100%);
    background-size: 200% 200%;
    animation: bannerGradient 12s ease infinite;
    overflow: hidden;
    color: #fff;
  }
  
  @keyframes bannerGradient {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }
  
  /* ===== BACKGROUND ELEMENTS ===== */
  .quote-bg-elements {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }
  
  .quote-glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.3;
  }
  
  .quote-glow-1 {
    width: 400px;
    height: 400px;
    background: #95cca5;
    top: -100px;
    left: -100px;
    animation: glowFloat 8s ease-in-out infinite;
  }
  
  .quote-glow-2 {
    width: 300px;
    height: 300px;
    background: #4a8fc2;
    bottom: -80px;
    right: -80px;
    animation: glowFloat 10s ease-in-out infinite reverse;
  }
  
  @keyframes glowFloat {
    0%, 100% {
      transform: translate(0, 0) scale(1);
    }
    50% {
      transform: translate(30px, -20px) scale(1.1);
    }
  }
  
  .quote-particles span {
    position: absolute;
    width: 4px;
    height: 4px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    animation: particleFloat 15s linear infinite;
  }
  
  .quote-particles span:nth-child(1) { left: 10%; top: 20%; animation-delay: 0s; }
  .quote-particles span:nth-child(2) { left: 30%; top: 80%; animation-delay: 3s; }
  .quote-particles span:nth-child(3) { left: 50%; top: 40%; animation-delay: 6s; }
  .quote-particles span:nth-child(4) { left: 70%; top: 70%; animation-delay: 9s; }
  .quote-particles span:nth-child(5) { left: 90%; top: 30%; animation-delay: 12s; }
  
  @keyframes particleFloat {
    0% {
      transform: translateY(100px) rotate(0deg);
      opacity: 0;
    }
    10% {
      opacity: 0.5;
    }
    90% {
      opacity: 0.5;
    }
    100% {
      transform: translateY(-100px) rotate(360deg);
      opacity: 0;
    }
  }
  
  /* ===== CONTENT ===== */
  .quote-content {
    position: relative;
    z-index: 2;
    max-width: 900px;
  }
  
  .quote-title {
    font-size: clamp(1.5rem, 4vw, 2.25rem);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    line-height: 1.4;
    margin-bottom: 16px;
    color: #fff;
  }
  
  .quote-line {
    display: block;
    opacity: 0;
    transform: translateY(20px);
  }
  
  .quote-content.visible .quote-line:nth-child(1) {
    animation: revealQuoteLine 0.8s ease forwards 0.2s;
  }
  
  .quote-content.visible .quote-line:nth-child(2) {
    animation: revealQuoteLine 0.8s ease forwards 0.5s;
  }
  
  @keyframes revealQuoteLine {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .quote-highlight {
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    font-weight: 900;
    margin: 0;
    color: #95cca5;
    opacity: 0;
    transform: scale(0.9);
    text-shadow: 0 0 30px rgba(149, 204, 165, 0.5);
  }
  
  .quote-content.visible .quote-highlight {
    animation: revealHighlight 0.8s ease forwards 0.9s;
  }
  
  @keyframes revealHighlight {
    to {
      opacity: 1;
      transform: scale(1);
    }
  }
  
  /* Hover enhancement */
  .quote-banner:hover .quote-highlight {
    text-shadow: 0 0 50px rgba(149, 204, 165, 0.7);
    transition: text-shadow 0.5s ease;
  }
  
  @media (max-width: 768px) {
    .quote-banner {
      padding: 60px 20px;
      min-height: 260px;
    }
    
    .quote-glow-1 {
      width: 200px;
      height: 200px;
    }
    
    .quote-glow-2 {
      width: 150px;
      height: 150px;
    }
  }
</style>

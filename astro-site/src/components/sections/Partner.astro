---
// Partner section - Styled like "Así Trabajamos" social proof but with image
---

<section class="section partner-section" id="partner">
  <!-- Animated Gradient Background (Matches Asi Trabajamos) -->
  <div class="hero-gradient"></div>
  
  <!-- Floating Particles -->
  <div class="floating-particles">
    <span class="particle"></span>
    <span class="particle"></span>
    <span class="particle"></span>
    <span class="particle"></span>
    <span class="particle"></span>
  </div>

  <!-- Top Wave Divider -->
  <div class="shape-divider-top">
    <svg data-name="Layer 1" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 120" preserveAspectRatio="none">
      <path d="M321.39,56.44c58-10.79,114.16-30.13,172-41.86,82.39-16.72,168.19-17.73,250.45-.39C823.78,31,906.67,72,985.66,92.83c70.05,18.48,146.53,26.09,214.34,3V0H0V27.35A600.21,600.21,0,0,0,321.39,56.44Z" class="shape-fill"></path>
    </svg>
  </div>

  <div class="cesis_container z-relative">
    <div class="proof-card glass-effect">
      <div class="proof-content">
        
        <!-- Image Column -->
        <div class="image-wrapper animate-fade-in">
          <img 
            src="/images/Ilustracion_30-768x542.jpg" 
            alt="Ilustración confianza clientes"
            width="768"
            height="542"
            loading="lazy"
            class="feature-image"
          >
          <div class="image-glow"></div>
        </div>

        <!-- Text Column -->
        <div class="text-content animate-fade-in delay-200">
          <div class="badge-pill">Trayectoria</div>
          
          <h2 class="premium-heading">
            Somos un partner para todas las agencias y consultoras que han confiado en nuestra experiencia desde 2015
          </h2>
          
          <div class="feature-divider"></div>
          
          <p class="premium-text">
            Y seguimos creciendo cada año con nuevos clientes satisfechos.
          </p>

          <div class="action-buttons">
            <a href="/asi-trabajamos" class="btn-premium btn-wide btn-secondary-style">
              <span class="btn-icon">→</span>
              <span>Así trabajamos</span>
            </a>

            <button type="button" onclick="window.openContactModal()" class="btn-premium btn-wide">
              <span class="btn-icon">✉</span>
              <span>Contactar ahora</span>
              <div class="btn-glow"></div>
            </button>
          </div>
        </div>

      </div>
    </div>
  </div>

  <!-- Bottom Wave Divider -->
  <div class="shape-divider-bottom">
    <svg data-name="Layer 1" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 120" preserveAspectRatio="none">
      <path d="M321.39,56.44c58-10.79,114.16-30.13,172-41.86,82.39-16.72,168.19-17.73,250.45-.39C823.78,31,906.67,72,985.66,92.83c70.05,18.48,146.53,26.09,214.34,3V0H0V27.35A600.21,600.21,0,0,0,321.39,56.44Z" class="shape-fill"></path>
    </svg>
  </div>
</section>

<style>
  .partner-section {
    position: relative;
    padding: 160px 0;
    z-index: 2; /* Ensure wave stays on top of MiddleGround */
    background: #0a1628;
    color: #fff;
    overflow: hidden;
  }
  
  /* ANIMATIONS */
  .animate-fade-in {
    opacity: 0;
    animation: fadeInUp 0.8s ease-out forwards;
  }
  
  .delay-200 {
    animation-delay: 0.2s;
  }
  
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .z-relative {
    position: relative;
    z-index: 10;
  }

  /* BACKGROUND EFFECTS (Shared with Asi Trabajamos) */
  .hero-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg, 
      #0a1628 0%, 
      #1a3865 25%, 
      #0f2240 50%, 
      #1a3865 75%, 
      #0a1628 100%
    );
    background-size: 400% 400%;
    animation: gradientShift 12s ease-in-out infinite;
  }
  
  @keyframes gradientShift {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .floating-particles {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }
  
  .particle {
    position: absolute;
    width: 6px;
    height: 6px;
    background: rgba(149, 204, 165, 0.4);
    border-radius: 50%;
    animation: float 8s ease-in-out infinite;
  }
  
  .particle:nth-child(1) { left: 10%; top: 20%; animation-delay: 0s; }
  .particle:nth-child(2) { left: 80%; top: 30%; animation-delay: 1.5s; }
  .particle:nth-child(3) { left: 30%; top: 70%; animation-delay: 3s; }
  .particle:nth-child(4) { left: 60%; top: 80%; animation-delay: 4.5s; }
  .particle:nth-child(5) { left: 90%; top: 60%; animation-delay: 6s; }
  
  @keyframes float {
    0%, 100% { transform: translateY(0) translateX(0) scale(1); opacity: 0.3; }
    50% { transform: translateY(-40px) translateX(-20px) scale(1.5); opacity: 0.8; }
  }

  /* SHAPE DIVIDERS */
  .shape-divider-top {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    overflow: hidden;
    line-height: 0;
  }
  
  .shape-divider-top svg {
    position: relative;
    display: block;
    width: calc(100% + 1.3px);
    height: 80px;
    transform: rotateY(180deg);
  }
  
  .shape-divider-top .shape-fill {
    fill: var(--color-bg-body, #ffffff);
  }
  
  :global([data-theme="dark"]) .shape-divider-top .shape-fill {
    fill: var(--color-bg-body, #1a1a1a);
  }

  .shape-divider-bottom {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    overflow: hidden;
    line-height: 0;
    transform: rotate(180deg);
  }
  
  .shape-divider-bottom svg {
    position: relative;
    display: block;
    width: calc(100% + 1.3px);
    height: 80px;
    transform: rotateY(180deg);
  }
  
  .shape-divider-bottom .shape-fill {
    fill: #ffffff; /* Forced white to match MiddleGround */
  }

  :global([data-theme="dark"]) .shape-divider-bottom .shape-fill {
    fill: var(--color-primary);
  }

  /* GLASS CARD */
  .proof-card {
    position: relative;
    border-radius: 30px;
    padding: 60px;
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
    overflow: hidden;
    transition: transform 0.3s ease;
  }
  
  .glass-effect {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .proof-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
  }
  
  .proof-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }

  /* IMAGE STYLES */
  .image-wrapper {
    position: relative;
    border-radius: 20px;
    overflow: hidden;
    transform: perspective(1000px) rotateY(2deg);
    transition: transform 0.5s ease;
  }
  
  .feature-image {
    width: 100%;
    height: auto;
    border-radius: 20px;
    display: block;
    transition: transform 0.5s ease;
  }
  
  .proof-card:hover .image-wrapper {
    transform: perspective(1000px) rotateY(0deg) translateY(-5px);
  }
  
  .proof-card:hover .feature-image {
    transform: scale(1.03);
  }
  
  .image-glow {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, rgba(149, 204, 165, 0.2), transparent 70%);
    opacity: 0;
    transition: opacity 0.5s ease;
    pointer-events: none;
  }
  
  .image-wrapper:hover .image-glow {
    opacity: 1;
  }

  /* TEXT CONTENT */
  .text-content {
    text-align: left;
  }

  .badge-pill {
    display: inline-block;
    padding: 6px 16px;
    background: rgba(149, 204, 165, 0.15);
    border: 1px solid rgba(149, 204, 165, 0.3);
    color: #95cca5;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    border-radius: 50px;
    margin-bottom: 24px;
  }

  .premium-heading {
    font-family: 'Raleway', sans-serif;
    font-size: clamp(24px, 4vw, 36px);
    font-weight: 700;
    color: #ffffff;
    line-height: 1.3;
    margin-bottom: 30px;
  }

  .feature-divider {
    width: 80px;
    height: 3px;
    background: linear-gradient(90deg, #95cca5, transparent);
    margin-bottom: 30px;
  }

  .premium-text {
    font-size: 18px;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.85);
    margin-bottom: 40px;
    font-weight: 300;
  }

  /* BUTTON */
  .btn-premium {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    /* Default base styles, colors handled by modifiers below */
    padding: 16px 32px;
    border-radius: 50px;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    cursor: pointer;
    font-size: 1rem;
    line-height: 1.5;
  }

  .btn-wide {
    min-width: 200px;
  }

  .btn-premium:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 20px rgba(149, 204, 165, 0.3);
    color: #ffffff; /* Added for hover effect */
  }

  .btn-glow {
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(255,255,255,0.4), transparent 60%);
    opacity: 0;
    transform: scale(0.5);
    transition: transform 0.5s, opacity 0.5s;
  }

  .btn-premium:hover .btn-glow {
    opacity: 1;
    transform: scale(1);
    transition: transform 0.5s, opacity 0.5s;
  }
  
  .action-buttons {
    display: flex;
    gap: 20px;
    flex-wrap: wrap;
  }
  
  /* Primary Button (Green BG, Dark Text) */
  .btn-premium {
    background: linear-gradient(135deg, #95cca5 0%, #7ab892 100%);
    color: #1a3865;
    border: 2px solid transparent;
  }
  
  /* Secondary Button (Inverse: Transparent/Dark BG, Green Text, Green Border) */
  .btn-secondary-style {
    background: rgba(255, 255, 255, 0.05); /* Slight glass tint */
    border: 2px solid #95cca5;
    color: #95cca5;
  }
  
  .btn-secondary-style:hover {
    background: #95cca5;
    color: #1a3865;
    transform: translateY(-2px);
    box-shadow: 0 10px 20px rgba(149, 204, 165, 0.2);
  }
  


  /* RESPONSIVE */
  @media (max-width: 991px) {
    .proof-content {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 40px;
    }
    
    .text-content {
      text-align: center;
      order: 1; /* Text first on mobile? Or image? Let's keep standard stack order */
      order: 2;
    }
    
    .image-wrapper {
      order: 1;
      max-width: 500px;
      margin: 0 auto;
    }

    .feature-divider {
      margin: 0 auto 30px;
    }
    
    .proof-card {
      padding: 40px 24px;
    }
  }
</style>
